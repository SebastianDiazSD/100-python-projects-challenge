class Question:

    def __init__(self, q_text, q_answer, q_category,q_choices):
        self.text = q_text
        self.answer = q_answer
        self.category = q_category
        self.choices=q_choices
